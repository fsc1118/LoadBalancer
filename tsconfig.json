{
    "compilerOptions": {
        "target": "esnext", /* Set the target ECMAScript version. */
        "module": "commonjs", /* Specify module code generation. */
        "outDir": "./dist", /* Redirect output structure to the directory. */
        "rootDir": "./src", /* Specify the root directory of the TypeScript source files. */
        "strict": true, /* Enable all strict type-checking options. */
        "esModuleInterop": true, /* Allows importing CommonJS modules using ES6 import syntax. */
        "forceConsistentCasingInFileNames": true, /* Enforces consistent casing in file names (important for case-sensitive file systems). */
        "declaration": true, /* Generate declaration files (e.g., .d.ts). */
        "sourceMap": true /* Generate source maps for better debugging experience. */
    },
    "include": [
        "src/**/*.ts" /* Include TypeScript files under the "src" directory. */
    ],
    "exclude": [
        "node_modules", /* Exclude the "node_modules" directory from TypeScript compilation. */
    ]
}